{
  "name": "@spatial/workbench-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright browser tests for the browser workbench. Driven by eng/workbench-e2e.sh against the real host serving the built workbench.",
  "type": "module",
  "scripts": {
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0"
  }
}