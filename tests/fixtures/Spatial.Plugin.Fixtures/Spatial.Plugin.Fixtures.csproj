<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Phase 5 fault-fixture plugin: an out-of-process capability provider the
    worker-host tests package (manifest + assembly) and spawn. Implements
    Spatial.PluginSdk only — plugin implementations never reference the
    worker protocol; the worker host loads them by reflection (plan §16
    Phase 5, ADR-0006/0013).
  -->

  <ItemGroup>
    <ProjectReference Include="..\..\..\src\Spatial.Core\Spatial.Core.csproj" />
    <ProjectReference Include="..\..\..\src\Spatial.PluginSdk\Spatial.PluginSdk.csproj" />
  </ItemGroup>

</Project>