# Contracts

Language-neutral contract definitions: geometry, features, capabilities,
schemas and (later) WIT component descriptors. Nothing here depends on .NET
or any implementation language.

Planned with Phase 3 (capability descriptors), Phase 5 (worker protocol) and
Phase 12 (WebAssembly/WIT).