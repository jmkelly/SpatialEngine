# Clients

Generated and hand-written client SDKs for the public spatial host API:

- `typescript/` — SDK for the React workbench and web clients (Phase 9–10)
- `dotnet/` — SDK for automation and service clients (Phase 9)
- `cli/` — command-line client (later)

Clients are engine-neutral: they carry no geometry logic.